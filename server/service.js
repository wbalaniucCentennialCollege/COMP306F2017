'use strict';

const express = require('express'); // Imports 'express' package into a variable called express
const service = express(); // Top-level function (sort of like an init or constructor). Creates an express application

module.exports = service; 